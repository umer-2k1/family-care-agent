import type { CareDashboard } from "@/server/repositories/care";
import { ArrowRight, CalendarPlus, Check, Clock3, FilePlus2, HeartPulse, Sparkles } from "lucide-react";
import Link from "next/link";

export function TodayDashboard({ care }: { care: CareDashboard | null }) {
  const nextDose = care?.doses.find((dose) => dose.status === "scheduled");
  const completed = care?.doses.filter((dose) => dose.status === "taken").length ?? 0;
  return <div className="pb-20 lg:pb-0"><section className="flex flex-col gap-5 border-b border-border pb-8 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-primary-dark">Good afternoon</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Here&apos;s your family&apos;s care today.</h1><p className="mt-3 text-sm text-muted-foreground">Persisted care actions and follow-ups appear here.</p></div><div className="flex gap-3"><Link href="/care" className="inline-flex items-center gap-2 rounded-xl border border-border bg-surface px-4 py-2.5 text-sm font-semibold"><FilePlus2 size={17}/> Add health record</Link><Link href="/ask" className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-white"><Sparkles size={17}/> Ask Care Memory</Link></div></section>{care ? <section className="mt-8 grid gap-6 xl:grid-cols-[1.5fr_0.85fr]"><div className="space-y-6"><article className="rounded-3xl border border-border bg-surface p-6 shadow-card"><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-primary-soft text-primary-dark"><HeartPulse size={22}/></div><div><p className="font-semibold">{care.memberName}&apos;s care today</p><p className="text-sm text-muted-foreground">{care.title} · {care.medicationName}</p></div></div><div className="mt-6 flex items-center gap-4 rounded-2xl bg-muted p-4">{nextDose ? <><div className="grid size-9 place-items-center rounded-full border-2 border-primary-light"><Clock3 size={16}/></div><div className="flex-1"><p className="font-semibold">{care.medicationName} · {care.dose} {care.unit}</p><p className="text-sm text-muted-foreground">{new Date(nextDose.scheduledAt).toLocaleString()}</p></div><Link href="/care" className="text-sm font-semibold text-primary-dark">Record dose <ArrowRight size={15} className="inline"/></Link></> : <><Check className="text-primary-dark"/><p>All scheduled doses are recorded.</p></>}</div><p className="mt-4 text-sm text-muted-foreground">{completed} of {care.doses.length} doses taken</p></article></div><aside className="rounded-3xl border border-[#D6E2DD] bg-[#F0F6F3] p-6">{care.followUp ? <><CalendarPlus className="text-primary-dark"/><p className="mt-4 text-xs font-semibold uppercase tracking-wide text-primary-dark">Needs attention</p><h2 className="mt-2 text-lg font-semibold">{care.followUp.title}</h2><p className="mt-2 text-sm text-muted-foreground">Due {new Date(care.followUp.dueAt).toLocaleDateString()}</p><a href={`/api/calendar/google?followUpId=${care.followUp.id}`} className="mt-4 inline-flex rounded-xl bg-primary px-3.5 py-2.5 text-sm font-semibold text-white">Add to calendar</a></> : <p className="text-sm text-muted-foreground">No unresolved follow-ups.</p>}</aside></section> : <section className="mt-8 rounded-3xl border border-dashed border-primary-light bg-surface p-10 text-center"><HeartPulse className="mx-auto text-primary-dark"/><h2 className="mt-4 text-lg font-semibold">No active care episode</h2><p className="mt-2 text-sm text-muted-foreground">Upload and confirm a health record to create one.</p><Link href="/care" className="mt-5 inline-flex rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-white">Add health record</Link></section>}</div>;
}
